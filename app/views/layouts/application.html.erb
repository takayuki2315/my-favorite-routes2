<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MyFavoriteRoutes</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("taiyou.png", class: :logo_one), root_path %>
            <%= link_to image_tag("my_favorite_routes.png", class: :logo_two), root_path %>
          </div>
          <%# ログインしているときは以下を表示 %>
          <% if user_signed_in? %>
            <div class="nav__right">
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :nav__logout %>
              <%= link_to "新規投稿", new_route_path, class: :nav__btn %>
            </div>
          <% else %>
          <%# // ログインしているときは上記を表示 %>
          <%# ログインしていないときは以下を表示 %>
            <div class="nav__right">
              <%= link_to "ゲストログイン(閲覧用)", users_guest_sign_in_path, method: :post, class: :nav__btn2 %>
              <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
              <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
            </div>
          <% end %>
          <%# // ログインしていないときは上記を表示%>
        </div>
      </div>
    </header>
    
    <%= yield %>
    
    <footer class="footer">
      <p class="copyright">Copyright © MY FAVORITE ROUTES All rights reserved.</p>
    </footer>
  </body>
</html>
